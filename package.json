{
  "name": "@uasi-solutions/action-sharepoint-publish",
  "version": "1.1.1",
  "description": "GitHub Action that creates an archive of a repository and uploads it to a Sharepoint library",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "https://github.com/UASI-Solutions/action-sharepoint-publish",
  "license": "MIT",
  "devDependencies": {
    "spsave": "^3.1.6"
  }
}
